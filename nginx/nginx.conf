#server {
#    listen 80;
#    server_name co2-api.duckdns.org;

#    location / {
#        proxy_pass http://localhost:8081;  # Change to localhost or public IP of your VM
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#    }
#}

#server {
#    server_name co2-app.op-bit.nz;

#    root /usr/share/nginx/html/;
#    index index.html;

#    location / {
#        proxy_pass http://co2-frontend:8080;  # Ensure this matches the port exposed by the frontend container
#        proxy_set_header Host $host;
#        proxy_set_header X-Real-IP $remote_addr;
#        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#        proxy_set_header X-Forwarded-Proto $scheme;
#    }

#    listen [::]:443 ssl ipv6only=on; # managed by Certbot
#    listen 443 ssl; # managed by Certbot
#    ssl_certificate /etc/letsencrypt/live/co2-app.op-bit.nz/fullchain.pem;
#    ssl_certificate_key /etc/letsencrypt/live/co2-app.op-bit.nz/privkey.pem;

#    ssl_protocols TLSv1.2 TLSv1.3;
#    ssl_prefer_server_ciphers on;
#    ssl_dhparam /etc/ssl/certs/dhparam.pem;
#}

# Redirect HTTP to HTTPS
#server {
#    if ($host = co2-app.op-bit.nz) {
#        return 301 https://$host$request_uri;
#    }

#    listen 80 default_server;
#    listen [::]:80 default_server;
#    server_name co2-app.op-bit.nz;
#    return 404;
#}
